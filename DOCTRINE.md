# 技能设计教条

改这个合集里任何一个技能之前，先读这篇。

内容浓缩自 mattpocock 的 [`writing-great-skills`](https://github.com/mattpocock/skills/blob/main/skills/productivity/writing-great-skills/SKILL.md)（MIT），术语沿用原文。

---

## 根本目标：可预测性

技能存在的意义是**从一个随机系统里榨出确定性**。这里的"可预测"指的是**每次运行走同一个流程**，而不是产出同样的文字。下面所有的杠杆都服务于这一条。

## 两种调用方式，两种成本

| | 谁能触发 | 成本 |
|---|---|---|
| **模型调用**（不写 `disable-model-invocation`） | 模型自己 + 你 + 其他技能 | **上下文成本**：description 每轮都在窗口里 |
| **用户调用**（`disable-model-invocation: true`） | 只有你，打名字 | 不花常驻 description 成本（正文在你调用它时才进入上下文，之后留在会话里），但花**认知成本**：得由你记得它存在 |

只有当模型必须自己够到它、或别的技能必须够到它时，才选模型调用。只靠手动触发的，设成用户调用，不花常驻成本。

用户调用的技能多到你记不住时，那份堆积的认知成本用一个**路由技能**来治：一个用户调用的技能，列出其他技能各自什么时候用。本合集只有 9 个，还不需要。

## 写 description

模型调用的 description 干两件事：说清这技能是什么，列出该触发它的**分支**。每个词都增加上下文成本，所以 description 要比正文剪得更狠。

- **前置领头词** —— description 是它干触发这活儿的地方。
- **一个分支一个触发条件。** 用同义词把同一个分支写两遍是**重复**："用 TDD 构建功能……要求测试先行"是一个分支写了两次。合并掉。
- **删掉正文里已有的身份描述。** description 只留触发条件，外加"当别的技能需要……"这类可达性说明。

## 信息层级

技能由两种内容组成 —— **步骤**和**参考** —— 可以自由混合。核心决策是用哪种、放在层级的哪一层（按"模型多急着要"排序）：

1. **技能内步骤** —— `SKILL.md` 里有序的动作，主层。每个步骤以一条**完成判据**收尾：告诉模型这步真的做完了的条件。判据要**可检查**（模型分得清做完没做完吗？），必要时要**穷尽**（"每个改动过的模型都被覆盖"，而不是"产出一份变更清单"）—— 模糊的判据招来**提前完成**。
2. **技能内参考** —— `SKILL.md` 里的定义、规则、事实，按需查阅。经常是合法的扁平同级集合（一次评审的所有规则放在同一层），这是合理安排，不是坏味道。
3. **外部参考** —— 从 `SKILL.md` 推到独立文件，靠**上下文指针**够到，指针触发时才加载。

判据越硬，**功课**做得越足 —— "每条规则都用上"约束扁平参考的力度，和"每步都做完"约束序列一样。

往下推太少，顶层臃肿；推太多，藏起了模型真正需要的东西。这个张力就是全部的决策。

**渐进披露**就是往下挪的动作。分支是最干净的判据：每条分支都要的内容内联，只有部分分支才够到的推到指针后面。指针的**措辞**（而非它指向什么）决定模型什么时候、多可靠地够到那些材料。

层级决定一段内容**往下放多远**，**就近原则**决定它落地之后**旁边坐着谁**：把一个概念的定义、规则、注意事项放在同一个标题下，读到一部分就带出它的邻居。

## 什么时候拆分

**粒度**是你把技能分得多细，每一刀都花掉两种成本之一，所以只有这一刀值回票价才切。两种切法：

- **按调用切** —— 当你有一个独立的**领头词**该单独触发它，或别的技能必须够到它。你为新增的常驻 description 付上下文成本，那份独立可达性得配得上。
- **按序列切** —— 当后面还没做的步骤会诱使模型草率处理眼前这步（**提前完成**）时，把后续步骤挪出视野，模型才会在当前任务上多做功课。

## 修剪

每个意思保持**单一真相**：一个权威位置，改行为是一处编辑。

逐行检查**相关性**：它还关系到这技能做的事吗？

然后逐句（不是逐行）猎杀**空转句**：把每句话单独拿出来做空转测试 —— 它相对默认行为改变了什么吗？没有就删掉整句，而不是修修词。要狠：大部分没通过测试的句子该删，不该重写。

## 领头词

**领头词**是一个已经活在模型预训练里的紧凑概念，模型运行技能时用它来思考（如 _lesson_、_fog of war_、_tracer bullets_）。在文本里重复出现，它积累起一个分布式定义，用最少的 token 锚定一整片行为区域 —— 因为它调用的是模型已经持有的先验。

它在两处服务可预测性。正文里锚定**执行**：每次这个词出现，模型伸手去够同一套行为。description 里锚定**触发**：当同一个词也活在你的 prompt、文档、代码里，模型会把这套共享语言和技能连起来，触发得更可靠。

主动去找可以重构成领头词的地方：

- "快、确定、低开销" → _tight_ —— 一个性质在一个阶段里被反复陈述，收进一个预训练词（一个 _tight_ 的回路）。
- "一个你信得过的回路" → _red_ —— 把模糊的闸门变成二元可观测状态（回路对这个 bug 变红，或者没有）。

赢两次：token 更少，**并且**给模型一个更锋利的思维挂钩。

## 失效模式

用来诊断技能出的问题：

- **提前完成** —— 一步没真做完就收尾，注意力滑向"完成"本身。防御顺序：先磨锋完成判据（便宜、局部）；只有当判据确实无法再精确**且**你观察到了这种草率，才用序列切分把后续步骤藏起来。
- **重复** —— 同一个意思出现在不止一处。费维护、费 token，还把这个意思在层级上的显著度抬到超过它的真实排位。
- **沉积** —— 陈旧层次的堆积，因为"加"感觉安全而"删"感觉危险。任何没有修剪纪律的技能的默认命运。
- **膨胀** —— 技能单纯太长，哪怕每一行都还活着、都不重复。伤可读性和可维护性，浪费 token。解药是层级：把参考披露到指针后面，按分支或序列拆分，让每条路径只扛自己要的东西。
- **空转** —— 模型默认就会遵守的一行字，你付了载荷却什么也没说。测试：它相对默认行为改变了什么吗？一个弱领头词（模型本来就挺周全时说 _be thorough_）就是空转；解法是换个更强的词（_relentless_），不是换个技术。
- **否定式** —— 用禁止来引导会反噬：_别想大象_ 点了大象的名，让它更容易被想起，而不是更难。**正向表述** —— 说出目标行为，让被禁的那个从头到尾没被说出口。只在实在无法正向表述的硬护栏上保留禁止，即便如此也要配上"那该做什么"。

---

_浓缩自 [mattpocock/skills · writing-great-skills](https://github.com/mattpocock/skills/blob/main/skills/productivity/writing-great-skills/SKILL.md)，MIT © 2026 Matt Pocock。建议对照完整原文及其 [GLOSSARY.md](https://github.com/mattpocock/skills/blob/main/skills/productivity/writing-great-skills/GLOSSARY.md) 阅读。_
